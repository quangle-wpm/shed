---
paths:
  - "config/**"
---

- Named exactly as deployed (e.g. `.gitconfig`, `.zshrc`, `starship.toml`)
- Flat layout — no subdirectories
- Source references only; linking/copying done manually outside this repo
